#include <iostream>
#include <set>

using namespace std;



struct edge {

    int from
    bool operator<( const event & o) const {
        if (end != o.end) return end < o.end;
    }
}


int init(int n , int m) 
{
    int a,b,w;
    for (int i=0 ; i<m ; i++)
    {
        cin >> a >> b >> w;
    }
}


int main() 
{

    int n,m;
    cin >> n >> m;
    
    init(n,m);
    
}